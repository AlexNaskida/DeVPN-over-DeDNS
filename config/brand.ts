/**
 * Single source of truth for product naming. Every component, contract,
 * package, CSS class, and copy string must reference these constants
 * rather than hardcoding either name, so a rename is a one-line change.
 */
export const PRODUCT_NAME_FULL = "DeVPN-over-DeDNS";
export const PRODUCT_NAME_SHORT = "DVoD";

/** ENS parent domain relay operators register subnames under, e.g. bob.dvod.eth */
export const ENS_PARENT_DOMAIN = "dvod.eth";
